x=0
y=0
clear
for i in $(seq 1 10000); do
	tput cup $y $x
	. ./export $1 ; ../push_swap $ARG > output
	cat output | wc -l | tr -d '\n'
	cat output | ../checker $ARG | tr -d '\n' | grep -v 'OK'
	y=$(($y + 1))
	if [[ $y -eq 80 ]]; then
		y=0
		x=$(($x + 10))
	fi
done
